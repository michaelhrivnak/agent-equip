.DS_Store
/node_modules

# compiled standalone binaries (bun build --compile) + its scratch files
/dist
*.bun-build

# editors
/.idea
/.vscode
/.zed
/.fleet
/.nova

# dogfood artifacts (this repo installs itself; *.agent-equip-new are install previews)
*.agent-equip-new

# agent-equip >>>
/CLAUDE.local.md
/.claude/settings.local.json
*.agent-equip-new
# agent-equip <<<
